#summary Example commands and tasks GoogleCL can do.

*Note:* I've replaced `$ ./google.py` with `> ` for simplicity's sake.
This document is current as of at least r107

===Export contacts to a spreadsheet===
Code:
{{{
> contacts list title,email > my_contacts.csv
> docs upload my_contacts.csv
}}}